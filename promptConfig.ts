/**
 * @file promptConfig.ts
 * @description 提示词配置管理系统
 * 
 * 这个文件定义了所有可编辑的提示词配置
 * 管理员可以通过管理界面查看和编辑这些提示词
 */

/**
 * 提示词配置项接口
 */
export interface PromptConfigItem {
    id: string;                    // 唯一标识
    name: string;                  // 显示名称
    category: string;              // 分类
    description: string;           // 说明
    content: string;               // 提示词内容
    editable: boolean;             // 是否可编辑
    version: string;               // 版本号
    lastModified?: number;         // 最后修改时间
}

/**
 * 提示词分类
 */
export const PROMPT_CATEGORIES = {
    STRUCTURE: '结构化提示词',
    MAP_TYPE: '导图类型专项',
    GENERATION: '内容生成',
    ANALYSIS: '文本分析',
    OPTIMIZATION: '优化处理',
    CONVERSION: '格式转换'
} as const;

/**
 * 默认提示词配置
 * 这些配置会在首次加载时写入localStorage
 * 之后可以通过管理界面修改
 */
export const DEFAULT_PROMPT_CONFIG: PromptConfigItem[] = [
    {
        id: 'mindmap_structure_base',
        name: '思维导图基础结构',
        category: PROMPT_CATEGORIES.STRUCTURE,
        description: '定义思维导图的JSON输出格式和基本结构规则',
        editable: true,
        version: '1.0.0',
        content: `========================================
【输出格式】: JSON (严格模式)
========================================

你必须返回一个代表根节点的 JSON 对象。
禁止用数组包裹,禁止用 {"root": ...} 这样的额外层级。

【目标结构示例】(注意多层级):
{
  "name": "根节点名称",
  "type": "{{rootType}}",
  "description": "简短概述(不超过100字)",
  "children": [
     { 
       "name": "子节点1", 
       "type": "{{childType}}", 
       "description": "简短描述(不超过80字)", 
       "children": [
         {
           "name": "二级子节点1-1",
           "type": "{{childType}}",
           "description": "更具体的描述",
           "children": []
         }
       ]
     }
  ]
}

========================================
【关键结构规则】(必须严格遵守):
========================================

1. ⚠️ 禁止在 description 中堆砌大量内容!
   - 每个节点的 description 必须简洁(50-100字)
   - 如果有多个要点,必须拆分成多个子节点
   - description 只用于概述,不要列举详细内容

2. ✅ 必须创建足够的子节点:
   - 根节点的 children 数组至少要有 4-8 个子节点
   - 每个子节点必须有 'type'='{{childType}}'
   - 子节点的 name 要具体明确,不要用"其他"、"更多"等模糊词

3. ✅ 必须创建多层级结构:
   - **目标层级深度: 3-4层** (这是重点!)
   - 如果某个子节点内容复杂,必须继续创建它的 children
   - 不要把复杂内容都写在 description 里,而是拆分成子节点
   - 至少50%的一级子节点应该有自己的二级子节点
   - 叶子节点的 children 可以是空数组 []

4. ⚠️ 严禁的错误做法:
   - ❌ 把所有内容写在根节点的 description 里
   - ❌ 只创建1-2个子节点,其他内容都塞在 description
   - ❌ 使用"包括但不限于"、"等等"这样的模糊表述
   - ❌ 子节点 name 重复或过于笼统
   - ❌ 只有一层子节点,没有继续展开

5. ✅ 层级展开策略:
   - 第1层: 主要分类 (4-8个节点)
   - 第2层: 具体项目 (每个分类下2-5个节点)
   - 第3层: 详细属性 (复杂项目下1-3个节点)
   - 第4层: 可选的更细节内容`
    },
    {
        id: 'mindmap_system',
        name: '力量体系导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '力量体系/修炼体系的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【力量体系专项要求】:
⚠️ 内容丰富度要求:
- 必须创建完整的等级体系,至少 8-12 个等级节点
- 每个等级必须有独特的能力和特征,不能千篇一律
- 必须包含特殊能力、稀有技能、禁术等吸引眼球的设定

✅ 结构要求:
- 每个等级节点下必须展开 4-6 个子节点:
  * 修炼条件(具体的资源、天赋要求)
  * 能力特征(独特的法术、技能)
  * 突破方法(关键的突破契机)
  * 战力表现(与其他等级的对比)
  * 稀有能力(该等级的特殊技能)
  * 修炼难度(时间、资源消耗)

🎯 精彩程度要求:
- 每个等级要有令人向往的独特能力
- 突破过程要有戏剧性和挑战性
- 高等级要有震撼性的力量展示
- 示例: "金丹期" -> ["凝聚金丹", "御剑飞行", "神识外放", "寿命延长至500年", "雷劫考验", "可炼制四品丹药"]`
    },
    {
        id: 'mindmap_world',
        name: '世界观导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '世界观设定的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【世界观专项要求】:
⚠️ 内容丰富度要求:
- 必须创建宏大而独特的世界观,避免套路化设定
- 地理要有特色地标、奇异地形、神秘区域
- 历史要有重大事件、传奇人物、未解之谜
- 势力要有复杂关系、明争暗斗、隐藏组织
- 法则要有独特的世界规则、禁忌、天道设定

✅ 结构要求(至少5个一级节点):
1. 地理 -> 至少4-6个区域,每个区域3-5个子节点
2. 历史 -> 至少3-5个重大历史时期
3. 势力 -> 至少5-8个主要势力
4. 法则 -> 世界运行规则
5. 特色设定 -> 让世界独一无二的元素

🎯 精彩程度要求:
- 每个区域要有吸引人的特色和故事
- 势力之间要有复杂的恩怨情仇
- 历史要为当前剧情埋下伏笔
- 法则要能产生戏剧冲突`
    },
    {
        id: 'mindmap_chapters',
        name: '章节细纲导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '章节细纲的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【章节细纲专项要求】:
⚠️ 内容丰富度要求:
- 必须创建完整的章节规划,形成连贯的故事线
- 每章要有明确的剧情推进和爽点设计
- 章节之间要有递进关系和悬念衔接
- 至少创建 15-25 个章节节点

✅ 结构要求(每个章节5-8个子节点):
1. 章节标题 - 要有吸引力和悬念感
2. 场景设定 - 地点、时间、氛围
3. 剧情概要 - 主线、支线、承上启下
4. 冲突设计 - 外部、内部、升级
5. 转折/高潮 - 意外、反转、爽点
6. 角色表现 - 行动、作用、成长
7. 悬念/伏笔 - 章末钩子
8. 爽点/看点 - 装逼打脸、收获、情感

🎯 精彩程度要求:
- 开篇3章必须有强烈钩子
- 每5章一个小高潮
- 每10章一个大高潮
- 每章结尾必须有悬念或爽点

📊 章节节奏建议:
- 快节奏(60%): 冲突、战斗、危机
- 慢节奏(25%): 修炼、感情、日常
- 转折(15%): 重大变故、剧情反转`
    },
    {
        id: 'mindmap_character',
        name: '角色档案导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '角色档案的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【角色档案专项要求】:
⚠️ 内容丰富度要求:
- 必须创建立体丰满的角色,避免脸谱化
- 每个角色要有独特的性格、动机、成长轨迹
- 角色之间要有复杂的关系网络
- 至少包含: 主角、核心配角(3-5个)、主要反派(2-3个)、关键配角(2-3个)

✅ 结构要求(每个角色至少6-8个子节点):
1. 基本信息 - 姓名、年龄、身份、外貌、修为
2. 性格特点 - 核心性格、缺陷、习惯、说话方式
3. 背景故事 - 出身、转折、创伤、秘密
4. 核心关系 - 与主角、与他人、情感、利益
5. 能力特长 - 天赋、绝技、宝物、隐藏实力
6. 动机目标 - 短期、长期、准则
7. 成长轨迹 - 初期、转变、走向
8. 角色魅力 - 特质、台词、高光时刻

🎯 精彩程度要求:
- 主角有独特金手指或成长路线
- 配角有鲜明个性,不是工具人
- 反派有合理动机,不是纯粹的恶
- 角色关系有张力和冲突`
    },
    {
        id: 'mindmap_events',
        name: '事件时间轴导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '事件时间轴的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【事件时间轴专项要求】:
⚠️ 内容丰富度要求:
- 必须创建完整的事件链,形成起承转合的故事线
- 事件之间要有因果关系和递进关系
- 必须包含高潮事件、转折事件、伏笔事件
- 至少创建 12-20 个事件节点,覆盖故事的主要发展

✅ 结构要求(每个事件5-7个子节点):
1. 事件概述 - 名称、时间、地点、角色
2. 起因 - 导火索、深层原因、前置铺垫
3. 经过 - 开端、发展、高潮、转折
4. 结果 - 表面、隐藏、角色变化
5. 影响 - 对主角、对格局、对剧情、伏笔
6. 精彩看点 - 爽点、冲突、反转、情感
7. 关键细节 - 道具、对话、线索

🎯 精彩程度要求(起承转合):
- 起: 开端有悬念或冲突
- 承: 发展有波折,不一帆风顺
- 转: 必须有意外转折
- 合: 结局有爽点或震撼,埋下新悬念

📊 事件类型分布建议:
- 成长事件(30%): 实力提升、获得机缘
- 冲突事件(40%): 对抗、生死危机
- 情感事件(15%): 友情、爱情、师徒情
- 转折事件(15%): 重大变故`
    },
    {
        id: 'mindmap_mission',
        name: '任务状态导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '任务状态的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【任务状态专项要求】:
- 每个阶段的任务/状态必须是独立的子节点
- 至少创建 5-8 个任务节点
- 每个任务包含: 任务名称、目标、奖励/后果`
    },
    {
        id: 'mindmap_anchor',
        name: '伏笔锚点导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '伏笔锚点的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【伏笔锚点专项要求】:
- 每个伏笔/关键物品必须是独立的子节点
- 至少创建 4-6 个伏笔节点
- 每个伏笔包含: 名称、埋设位置、揭示时机`
    },
    {
        id: 'mindmap_structure',
        name: '宏观结构导图',
        category: PROMPT_CATEGORIES.MAP_TYPE,
        description: '宏观结构的专项要求',
        editable: true,
        version: '1.0.0',
        content: `【宏观结构专项要求】:
- 必须将小说拆分成多个卷/篇章
- 每卷必须是独立的子节点
- 至少创建 3-5 个卷节点
- 每卷包含: 卷名、核心冲突、预期章节数`
    }
];

/**
 * 从localStorage加载提示词配置
 */
export const loadPromptConfig = (): PromptConfigItem[] => {
    const stored = localStorage.getItem('inkflow_prompt_config');
    if (stored) {
        try {
            return JSON.parse(stored);
        } catch (e) {
            console.error('Failed to parse prompt config:', e);
        }
    }
    // 首次加载,使用默认配置
    savePromptConfig(DEFAULT_PROMPT_CONFIG);
    return DEFAULT_PROMPT_CONFIG;
};

/**
 * 保存提示词配置到localStorage
 */
export const savePromptConfig = (config: PromptConfigItem[]): void => {
    localStorage.setItem('inkflow_prompt_config', JSON.stringify(config));
};

/**
 * 获取单个提示词配置
 */
export const getPromptById = (id: string): PromptConfigItem | undefined => {
    const config = loadPromptConfig();
    return config.find(item => item.id === id);
};

/**
 * 更新单个提示词配置
 */
export const updatePromptConfig = (id: string, updates: Partial<PromptConfigItem>): void => {
    const config = loadPromptConfig();
    const index = config.findIndex(item => item.id === id);
    if (index !== -1) {
        config[index] = {
            ...config[index],
            ...updates,
            lastModified: Date.now()
        };
        savePromptConfig(config);
    }
};

/**
 * 重置提示词配置为默认值
 */
export const resetPromptConfig = (): void => {
    savePromptConfig(DEFAULT_PROMPT_CONFIG);
};

/**
 * 导出提示词配置为JSON
 */
export const exportPromptConfig = (): string => {
    const config = loadPromptConfig();
    return JSON.stringify(config, null, 2);
};

/**
 * 从JSON导入提示词配置
 */
export const importPromptConfig = (jsonString: string): boolean => {
    try {
        const config = JSON.parse(jsonString);
        if (Array.isArray(config)) {
            savePromptConfig(config);
            return true;
        }
    } catch (e) {
        console.error('Failed to import prompt config:', e);
    }
    return false;
};
